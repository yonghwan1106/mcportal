# 15102108 — 관세청 수출입총괄

월 범위로 우리나라 전체의 수출·수입 건수·금액·무역수지를 조회하는 오퍼레이션
1개짜리 프리셋이다. 국가 구분이 없다는 점만 [`15101612`](../15101612/README.md) 와
다르다. **응답 스키마 1건은 v0.2.0 에서 2026-08-09 실키 샘플 1건으로 확정했다.**

| 항목 | 값 |
|---|---|
| 소스 종류 | `gw_swagger`(Swagger 2.0) |
| base_url | `https://apis.data.go.kr/1220000/Newtrade` |
| 오퍼레이션 | 1 (`GET /getNewtradeList`) |
| 응답 스키마 미확정 | **0 / 1** |
| 응답 스키마 확정 방법 | 실키 샘플 1건(2026-08-09) → `sampled_schemas.json` |
| 묶음(`service.group`) | 관세청 수출입무역통계 |
| 짝 데이터셋 | [`15101612` 국가별 수출입실적](../15101612/README.md) |

## 1. 스펙 출처

| 항목 | 내용 |
|---|---|
| 상세 페이지 | https://www.data.go.kr/data/15102108/openapi.do |
| 스펙 URL | **없음** — 상세 페이지 HTML 안 `const swaggerJson` 템플릿 리터럴에 문서 전문이 인라인되어 있다 |
| 취득일(KST) | 2026-08-06 |
| 취득 방법 | **무인증 HTTP GET** 후 JS 템플릿 리터럴 규칙으로 언이스케이프 → 파싱. 인증키 사용 0회, 게이트웨이 데이터 호출 0회 |
| 원문 파일 | `presets/_raw/15102108/datagokr_gw_swagger_15102108.raw.json` (7,795 B) |
| 참고문서 | `15101612` 와 동일 파일이라 중복 저장하지 않았다 (`presets/_raw/15101612/refdoc_관세청조회코드_v1.2.xlsx`) |

## 2. 이용조건·트래픽

| 항목 | 값 |
|---|---|
| 이용허락범위 | **제한 없음** |
| 개발단계 / 운영단계 | 자동승인 / 자동승인 |
| 개발계정 트래픽 | 10,000회/일 |
| 데이터 포맷 | XML 단독 |
| 비용 | 무료 |

## 3. 짝 데이터셋과의 관계 (큐레이션 핵심)

두 관세 프리셋은 파라미터 이름도, 응답 항목 이름도 거의 같다. 자동생성 설명만으로는
LLM 이 "국가별"과 "총괄"을 고르지 못한다. 그래서 **양쪽 설명에 서로를 가리키는
문장을 넣었다.**

| | `15101612` 국가별 | `15102108` 총괄(이 번들) |
|---|---|---|
| 국가 코드 파라미터 | 있음(선택) | **없음** |
| 응답의 국가 코드·국가명 항목 | 있음 | **없음** |
| 골라야 할 때 | 특정 국가와의 실적, 국가별 순위·비교 | 국가 구분 없는 전체 총계, 월별 추이 |

## 4. 큐레이션이 바꾼 것 / 바꾸지 않은 것

**바꾼 것**

- 서비스 설명을 3문장으로 압축(원 `info.description` 은 무역통계 개론이다). 원문은
  `source.json` 의 `document.info.description` 에 그대로 있다.
- 오퍼레이션 요약·설명 + 짝 데이터셋 상호 참조 문장.
- 예시 프롬프트 2개, 파라미터 힌트 2종(시작·종료 연월).
- 응답 스키마를 **미확정으로 강등**(근거는 §5).

**바꾸지 않은 것**

- 파라미터의 타입·필수 여부, 경로, 메서드, base_url.
- 필수 파라미터의 예시값 — **어댑터가 채운다**(§6).

## 5. 응답 스키마를 미확정으로 강등한 이유 — 그리고 실측 결과 (정찰 F-06 적중)

`15101612` 와 동일하다. 원 스펙의 200 응답 스키마는 (1) 실제 XML 보다 루트 래퍼가
한 겹 얕고, (2) 반복되는 항목 엘리먼트의 배열성을 표기하지 않았다고 보고 미확정으로
강등했었다.

**2026-08-09 실키 샘플 1건으로 둘 다 사실로 확인됐다.** 실제 정규화 결과는 루트
`response` 아래에 `header`(`resultCode`·`resultMsg`)와 `body` 가 들어가 선언보다
한 겹 깊었고, `body.items.item` 은 **배열**이었다.

`response.unresolved` 는 **그대로 유지한다** — 소스 선언을 믿지 않겠다는 판단이
실측으로 강화됐기 때문이다. 도구에 실리는 것은 선언 스키마가 아니라 실측 추론
스키마(`sampled_schemas.json`)이며, 산출물의 `info.x-mcportal.generation_mode` 가
`sampled`, `info.x-mcportal.schema_inference.unresolved` 는 이제 `0` 이다.

## 6. 실데이터의 지저분함(기록)

- `host` 에 경로가 들어 있고 `basePath` 가 빈 문자열이다(Swagger 2.0 위반). 포털
  관용 표기로 인정한다. 표준 검증기는 **산출 문서에만** 적용한다.
- **`definitions` 에 선행 공백이 붙은 오타 키(`' item'`)가 정상 키(`'item'`)와 함께
  존재한다.** `paths` 에서 참조되지 않아 무해하며, 실데이터가 얼마나 지저분한지를
  보여 주는 사례로 원문 그대로 보존한다. 두 정의는 내용도 미묘하게 다르다(오타 키
  쪽은 금액 항목 설명이 "수출미화금액"·"수입미화금액"이다).
- 표준 `parameters` 에 `example`·`default` 가 하나도 없고, 실제 예시값은 포털
  게이트웨이의 비표준 메타 블록에만 있다. 어댑터가 포털 공통 포맷으로 흡수해 빈
  `example` 만 채운다.
- 스칼라 리프마다 빈 `"properties": {}` 가 덧붙어 있다(정규화로 제거).
- **항목 배열에 합계 행이 섞여 온다.** 2026-08-09 실측(단일 월 조회)에서 반환된
  2건 중 하나는 `year` 가 `"2016.01"` 인 월 행이고, 나머지 하나는 `year` 자리에
  날짜가 아니라 `"총계"` 라는 한국어 낱말이 들어간 합계 행이었다. 같은 배열에
  데이터 행과 집계 행이 섞이므로 **소비자가 `year` 를 날짜로 파싱하면 깨진다.**
  스펙 어디에도 이 동작에 대한 설명이 없다.

## 7. 2026-08-09 실키 샘플로 확정한 항목

1. **응답 XML 의 루트 래퍼 한 겹과 반복 엘리먼트의 배열성** — 확정(§5).
2. **인증키 위치(query/header) 표현** — 중간표현이 `source.key_location` 으로
   표현한다(정찰 F-08, v0.2.0 반영). 이 프리셋은 기본값 `query` 다.

미해결로 남은 항목은 없다.

## 8. 재생성

```
mcportal compile 15102108
```

결과는 **바이트 동일**해야 한다.
