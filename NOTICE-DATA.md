# NOTICE-DATA — 데이터 출처·라이선스 고지

이 문서는 MCPortal 리포에 커밋되는 **데이터 유래 파일**(테스트 픽스처, 응답 샘플,
컴파일된 스펙 산출물)의 출처와 라이선스를 추적한다. 코드 라이선스는
[`LICENSE`](LICENSE)·[`NOTICE`](NOTICE)가 다루며, 이 문서는 **코드가 아닌 데이터**만
대상으로 한다. 둘을 분리하는 이유는 공공데이터의 이용조건(KOGL 등)이 Apache-2.0과
별개 규범이고, 출처 표시 의무가 파일 단위로 발생하기 때문이다.

---

## 1. 현재 상태 (2026-08-06, W3 시점)

> **API를 호출해 받은 실응답 데이터는 0건이다.**
> **W3부터 공공 API의 스펙 메타데이터는 출처를 명기하고 커밋한다** — 정본은
> [`presets/NOTICE-DATA.md`](presets/NOTICE-DATA.md).

| 구분 | 경로 | 실응답 데이터 | 비고 |
|---|---|---|---|
| 테스트 픽스처 | `tests/fixtures/**` | ❌ 없음(100% 합성) | 가상 기관·가상 필드값·도메인은 `.invalid` |
| 테스트 내 리터럴 | `tests/test_*.py` | ❌ 없음(100% 합성) | 가상 응답 본문. URL은 아래 주석 참조 |
| 데모 컴파일 산출물 | `specs/demo/**` | ❌ 없음(100% 합성) | 합성 스펙 → 합성 샘플 |
| 벤치마크 결과 | `benchmarks/results/**` | ❌ 없음(100% 합성 입력) | 측정 수치·환경 메타만. 개인정보 0 |
| **실 서비스 프리셋** | **`presets/**`** | ❌ 없음(**스펙 메타데이터만**) | 무인증 취득 · 인증키 사용 0회 · 게이트웨이 데이터 호출 0회. **§1-1 참조** |

무키 재현 경로(record/replay 카세트, 컴파일 데모)는 전부 합성 픽스처로 성립한다.

### 1-1. 프리셋에 커밋된 것은 무엇인가

`presets/` 에 들어간 것은 공공데이터포털이 **문서로 공개한 API 스펙**(스웨거 문서,
상세기능의 요청변수·출력결과 표)이며, 그 API를 **호출해서 받은 응답 데이터가
아니다.** 취득은 전부 무인증 HTTP GET/POST였고 인증키를 한 번도 쓰지 않았다.

스펙 문서 안에는 포털이 문서화용으로 적어 둔 **예시값**이 섞여 있다. 공통 규칙에
따라 그 부분은 원문 그대로 보존하고, 값 하나하나를 `presets/NOTICE-DATA.md` §2에
등재해 자리표시자임을 근거와 함께 밝혔다(실명·실 사업자등록번호·전화·이메일 0건).

파일별 출처 URL·취득일·이용허락범위·가공 여부, 공공누리 제1유형 출처표시 문구,
보존하지 않기로 한 자료와 그 사유는 전부
[`presets/NOTICE-DATA.md`](presets/NOTICE-DATA.md)가 정본이다. 원문 파일의
바이트 수·sha256 은 `presets/_MANIFEST_20260806.json` 에 있다.

**테스트 URL에 관한 정확한 사실**: W2에서 추가된 픽스처·테스트는 전부 `.invalid`
도메인을 쓴다. 다만 W1의 런타임 테스트 4개(`test_replay_scrub.py`,
`test_replay_cassette.py`, `test_runtime_cache.py`,
`test_transport_integration.py`)는 게이트웨이 호스트 문자열 `apis.data.go.kr` 을
그대로 쓴다 — 인증키 인코딩·스크러빙·캐시 키가 **실제 게이트웨이의 URL 형태**에
의존하므로 그 형태를 보존한 것이다. 경로(`/svc/list` 등)와 응답 본문은 전부
가상이며, 그 URL이 실제로 밖으로 나가는 일은 없다.

| 파일 | 그 URL을 무엇으로 쓰는가 | 차단 수단 |
|---|---|---|
| `test_replay_scrub.py` | 스크러빙 함수의 **입력 문자열**(요청을 만들지 않는다) | HTTP 자체가 없음 |
| `test_runtime_cache.py` | **캐시 키 문자열**(요청을 만들지 않는다) | HTTP 자체가 없음 |
| `test_replay_cassette.py` | 카세트 왕복용 요청 | `httpx.MockTransport` |
| `test_transport_integration.py` | 트랜스포트 통합 요청 | `respx` |

실측: PEP 578 감사 훅으로 **루프백이 아닌 모든 connect·이름 해석을 차단**한
상태에서 전체 스위트가 통과한다(2026-08-05 W2 스위트 `297 passed` / 2026-08-06 W3
스위트 `543 passed, 1 skipped`). 즉 **실호출·실데이터·실키는 0건**이다. 호스트
이름 문자열은 데이터가 아니므로 위 표의 판정은 유지된다.

### 합성 픽스처 규약

- 기관명·서비스명은 가상으로 짓는다(예: "가상행정연구원", "가상포털").
- 도메인은 RFC 2606 예약 TLD `.invalid` 를 쓴다(`https://apis.example.invalid/...`).
  실수로 실호출이 나가도 이름 해석 단계에서 막힌다.
- 서비스ID·좌표·주민 식별 가능 정보는 넣지 않는다.
- 합성 인증키는 `ab12+CD/34==`(디코딩키 형태) / `ab12%2BCD%2F34%3D%3D`(인코딩키
  형태)를 쓴다. 실키가 아니며, `+`·`/`·`=` 를 포함해 인코딩 변형이 서로 달라지므로
  스크러빙 검증에 쓰인다.

---

## 2. 실데이터 유래 파일을 커밋할 때의 기재 규약

실 API 응답으로 만든 카세트·샘플·스펙을 커밋하려면 **먼저 아래 표에 한 행을
등재한 뒤에만** 커밋한다. 등재되지 않은 실데이터 유래 파일은 리뷰에서 반려한다.

| # | 파일 경로 | 제공기관 | 데이터셋명 / 서비스ID | 이용조건(라이선스) | 취득일(KST) | 가공 여부 | 비고 |
|---|---|---|---|---|---|---|---|
| — | (없음) | — | — | — | — | — | 실응답 데이터 0건. 스펙 메타데이터는 `presets/NOTICE-DATA.md` §1 표가 정본 |

각 열의 작성 기준:

- **제공기관**: 데이터를 제공한 기관의 정식 명칭(포털 상세 페이지 표기 기준).
- **데이터셋명 / 서비스ID**: 공공데이터포털의 서비스명과 데이터셋 ID를 함께 적는다.
- **이용조건(라이선스)**: 포털 상세 페이지의 이용허락범위를 그대로 옮긴다(§3 참조).
  이용조건이 "이용허락범위 제한 없음"이 아닌 경우, 재배포 가능 여부를 **확인한
  뒤에만** 커밋한다.
- **취득일(KST)**: 응답을 실제로 받은 날짜. 재현 검증 시 스냅샷 시점을 특정한다.
- **가공 여부**: 스크러빙·발췌·필드 삭제 등 원본에 가한 변경을 요약한다. 인증키
  스크러빙(`__SCRUBBED__` 치환)은 **항상 적용**되므로 "키 스크러빙"으로 표기한다.

### KOGL 제1유형 수록 시 기재 형식

공공누리 제1유형(출처표시)은 출처 표시를 조건으로 상업적 이용·변형을 허용한다.
해당 파일을 수록할 때는 위 표에 행을 추가하고, 아래 문장을 이 문서 §4(출처 표시
목록)에 그대로 덧붙인다.

```
본 저작물은 "<제공기관명>"에서 <취득연도>년 작성하여 공공누리 제1유형으로
개방한 "<데이터셋명>"을 이용하였으며, 해당 저작물은 공공데이터포털
(https://www.data.go.kr)에서 무료로 내려받을 수 있습니다.
```

- `<제공기관명>`·`<데이터셋명>`은 포털 표기를 **글자 그대로** 옮긴다.
- 원본을 가공했다면 문장 끝에 "(가공: <가공 내용>)"을 덧붙인다.
- 제1유형이 아닌 유형(제2~4유형: 상업적 이용 금지·변형 금지 등)에 해당하는
  데이터는 **리포에 커밋하지 않는다.** 라이브 호출로만 다룬다.

---

## 3. 이용조건 확인 절차

1. 공공데이터포털의 해당 데이터셋 상세 페이지에서 **이용허락범위**를 확인한다.
2. 상세 페이지 표기와 별개로, 서비스 활용가이드 문서에 별도 이용조건이 적혀
   있으면 **더 제한적인 쪽**을 따른다.
3. 표기가 없거나 모호하면 재배포하지 않는다(라이브 호출 전용으로 남긴다).
4. 결론과 근거(확인 URL·확인일)를 §2 표의 비고 열에 남긴다.

---

## 4. 출처 표시 목록

공공누리 제1유형(출처표시) 대상은 프리셋 `15000115`(법제처 국가법령정보 공유서비스)
1건이다. 표준 출처표시 문구는
[`presets/NOTICE-DATA.md`](presets/NOTICE-DATA.md) §4에 적혀 있으며 그 문서가
**정본**이다. 프리셋 밖에는 출처표시 대상 파일이 없다.

---

## 5. 관련 문서

- [`LICENSE`](LICENSE) / [`NOTICE`](NOTICE) — 코드 라이선스(Apache-2.0)
- [`presets/NOTICE-DATA.md`](presets/NOTICE-DATA.md) — **프리셋 스펙 메타데이터·참고문서의 출처 정본**
- [`presets/README.md`](presets/README.md) — 프리셋 규약·2층 구조·미해결 항목
- [`specs/README.md`](specs/README.md) — 컴파일 산출물 커밋 규약
- [`README.md`](README.md) — 무키 경계(serviceKey 없이 되는 것 vs 실키가 필요한 것)
